# Deep Learning Git Hook DJ Dependencies
# Core ML dependencies
transformers>=4.21.0
torch>=1.12.0
tokenizers>=0.12.0

# Optional: for better model performance
accelerate>=0.20.0

# Note: The script will work without these dependencies using fallback heuristics
# Install with: pip install -r requirements.txt
